# Replace every negative number with 0.

arr = []
n = int(input("Enter the numbers of elements : "))

for i in range(n):
    arr.append (int(input("Ennter the number : ")))

for i in range(len(arr)):
    if arr[i] < 0 :
        arr[i] = 0
      
print(arr)                